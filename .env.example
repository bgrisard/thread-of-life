# Optional. Scripture, cross-references and commentaries work without this.
# Only the four written steps need it: the link note, the historical background,
# the Christ trace, and Ask.
#
# Copy this file to .env.local and paste your key.
VITE_ANTHROPIC_API_KEY=
